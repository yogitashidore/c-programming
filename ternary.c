//find maximum number useing ternary operator

#include<stdio.h>
 int main()
{
	//local variable declaration
	int a = 10;
	int b = 20;

	//code
	a > b ? printf("a is grater than b") : printf("b is grater than a");
	return(0);
}
